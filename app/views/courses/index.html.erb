<div class="row">
  <div class="col-md-12 col-sm-12 col-xs-12">
    <div class="x_panel">
      <div class="x_title">
        <h2>Cursos</h2>
        <div class="clearfix"></div>
      </div>
      <div class="x_content">

        <table class="table">
          <thead>
            <tr>
              <th>#</th>
              <th>Código i-Educar</th>
              <th>Nome</th>
            </tr>
          </thead>
          <tbody>
            <% @courses.each do |course| %>
            <tr>
              <th scope="row"><%= course.id %></th>
              <td><%= course.ieducar_code %></td>
              <td><%= course.name %></td>
            </tr>
            <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>
</div>